[phases.setup]
nixPkgs = ["python311", "espeak-ng"]   # espeak-ng: 地霊の日本語音声合成

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python seed.py"]

[start]
cmd = "uvicorn server.main:app --host 0.0.0.0 --port $PORT"
