# arigato - ありがとうアプリ

## 会話のはじめ方（毎回・2026-09-09）

**何も言われなくても、まずこの3つを読む**（読んだことは言わない）：

1. `docs/9月のタスク.md` ── **これが正**。日ごとのタスクと締めが `- [ ]` で並んでいる
2. `../../secretary/logs/` の最新ログ ── 昨日までに何があったか
3. （必要なら）`docs/9月の中スケジュール_2026-09-08.md` ── その週の大きな目標
4. `docs/いま触っているもの.md` ── **ほかのトークが並行して何を触っているか**（9/16追加）

**並行しているトークとぶつからないために（9/16）：**
- 手を動かす前に、触るもの（ファイル・ラズパイ本体・本番への入れ替え・C3）を `docs/いま触っているもの.md` の自分の行に書く
- ほかのトークの行にあるものは、**そのトークに確認してから**触る（ListAgents で名前を見て SendMessage）
- ラズパイ本体・本番への入れ替え（push）・C3への書き込みは、**同時に1トークだけ**
- 終わったら自分の行を消し、待っているトークに「終わった」と送る

**作業コピー（9/19）：作業トークは worktree、進行役だけが共通フォルダ**
- このディレクトリ（`arigato/`）で開いたトークは**進行役**。予定（`docs/9月のタスク.md`）・札・ログを持ち、コードは書かない
- **作業トークは自分で時刻に動けない。**「朝いちばん」「夕方に」の仕事は、**進行役がその時刻に呼びかけて初めて始まる**（9/22：呼びかけを忘れ、記録の窓の止血が半日遅れた）。進行役は会話を始めたら、まず各トークのその日の最初の仕事を送る
- 実装・実測をする作業トークは、**始めに `EnterWorktree`（name は役割名）で自分の作業コピーに移る**。手順と決まりは `docs/いま触っているもの.md` の「5. 作業コピー」
- 作業コピーに入ると編集の道具は共通フォルダ側を書けない。**札は進行役に1行送って代筆してもらう。ログは Bash の追記**（`cat >> C:/Users/kengk/research-os/secretary/logs/YYYY-MM-DD.md`）

**進行役に伝える（9/19 本人の希望。作業トークは言われなくても必ずやる）**
本人は「いま何が決まっていて、自分は何をすればいいか」を進行役の1か所で見る。作業トークの中だけで決まったことは、本人にも他のトークにも見えなくなる
（9/19：C3 の抜き差しは1つのトークの中だけで頼まれ、ほかのトークも進行役も経緯を知らず、話が食い違った）。
- **本人と何か決めたら、その場で進行役に1行送る**（何を・どう決めたか）
- **本人に何か頼んだら、進行役に1行送る**（頼んだ／済んだ／取り下げた）。物理的な手（電源・Wi-Fi・その場に行く）も、判断の待ちも
- **区切りがついたら進行役に1行送る**（終わった項目／push した／鍵を取った・放した／見つけた危ないもの）
- **時刻を書く前に必ず時計を読む**（`date '+%H:%M'`）。見積もりで書かない。9/19、あるトークの時刻が約45分先にずれ、他のトークの待ち時間や原因の切り分けを狂わせかけた
- 宛先は ListAgents に出る進行役の名前（`arigato/` で開いているトーク）。進行役が居なければ、札の「4. 本人の手が要るもの」とログに自分で書く

**そのうえで、こう切り出す（全体 → 昨日 → 今日 の順。長くしない）：**

```
【全体】一周の7段階：◯個つながり、残りは「◯◯」と「◯◯」。
　　　　手を動かせるのは10/4まで（あと◯日ぶん）。提出まであと◯週。

【昨日】◯/◯は「◯◯」まで進みました。残っているのは [◯◯]（あれば）。

【今日】◯/◯は「◯◯」の日。締めは「◯◯」。

始めますか？
```

- **全体は3行で足りる。** 一周の残り／手を動かせる残り／提出までの残り
- 一周の状態は `docs/9月のタスク.md` の「現在地」を見る。**進んだらそこも書き換える**

- 昨日の締めが**達成できていなければ、そう言う**。取り繕わない
- 今日が予備日なら、**遅れているものを提案する**
- タスクが1つ終わるたびに `docs/9月のタスク.md` の `- [ ]` を `- [x]` に変える
- **その日の締めに達したら、そう告げる。** それ以上は広げない（明日の分に手を出さない）

## 会話の終わり方

1. その日やったことをまとめて確認する
2. `../../secretary/logs/YYYY-MM-DD.md` に書く
3. 研究の証拠になる数字が出たら `【台帳#N】` を付ける（thesis側が拾う）
4. `docs/9月のタスク.md` のチェックが最新か確かめる

---

## プロジェクト概要
修士論文の研究用Webアプリ。
共有空間の整備記録を投稿し、ありがとうを届け合うシステム。

**GitHubリポジトリ：** https://github.com/kengiftology/arigato
**本番URL：** https://arigato-3ipecjbnha-an.a.run.app

---

## 研究の問い

「管理者なしに、ありがとうだけで共有空間の維持は持続するか」

### 前提の認識
- 今のゼミ室は教員の叱責（B）と掃除のおばちゃん（C）で維持されている
- 顔見知りの小さなコミュニティでさえ、互酬的なA（ありがとう）は自然に起きていない
- BとCがない空間でAだけが機能するかを観察する

### システムの役割
「埋もれているありがとうを掘り起こして届ける装置」
（ありがとうを人工的に生み出すのではなく、本来あったのに届かなかったものを通す）

### 2つのループ
```
基本ループ（既存の人を維持する）：
整備する → 投稿 → ありがとうが届く → また整備したくなる

新しいループ（新しい人を引き込む）：
積み重ねが見える → 自分もやりたくなる → 整備・投稿
```

新しいループの方が研究として面白い。③積み重ねの可視化がその核心。

### 理想の使われ方
- 整備した人が「記録を残す」
- 気づいた人が「誰がやったか確認してありがとうを送る」
- アプリを開くのは「誰かがやってくれたかも」と思ったとき

---

## 研究フィールド

両方を対象にする。ゾーンは使い始めてから自然に決める（先に決めない）。

| 場所 | 特徴 |
|------|------|
| 畑（SBCファーム） | B・Cなし、純粋にAだけ。人と動きは少ない |
| ゼミ室 | 人と動きはある。おばちゃんが来ない区画に絞る |

---

## アーキテクチャ

```
PWA（ブラウザ）
  ↕
FastAPI（Cloud Run / asia-northeast1）
  ↕ Firestore（整備記録・ありがとう・通知購読）
  ↕ GCS（写真 arigato-photos バケット）
```

### CI/CD
GitHub Actions → Cloud Run 自動デプロイ

### 環境変数（Cloud Run設定済み）
- `GCS_BUCKET` = arigato-photos
- `VAPID_EMAIL` / `VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY`

---

## 主要ファイル

| ファイル | 役割 |
|---------|------|
| `server/app.py` | FastAPIエントリポイント |
| `server/routers/maintenance.py` | 整備記録のCRUD |
| `server/routers/thanks.py` | ありがとうの送信（/welcomeを先頭に置くこと） |
| `server/routers/push.py` | プッシュ通知のVAPIDキー・購読登録 |
| `server/storage.py` | GCS写真アップロード |
| `server/push.py` | 実際の通知送信ロジック |
| `pwa/app.js` | フロントエンドロジック |
| `pwa/sw.js` | Service Worker（プッシュ通知受信） |

---

## 完了済み ✅
- 整備記録の投稿・表示
- 写真アップロード（GCS）
- ありがとうボタン（手動・自動）
- プッシュ通知（VAPID）
- ゾーン別フィルター（/zone/{zone_id}）

## 未実装・検討中 ❌
- [ ] ③積み重ねの可視化（コミュニティ全体の蓄積表示）
- [ ] NFCタグ連携（ESP32+PN532でNTAG215に書き込み）
- [ ] アドミンページ（ゾーンURL一覧表示）

---

## NFCタグ設計（予定）

各ゾーンにNTAG215タグを設置。かざすとそのゾーンの整備記録が開く。

- 書き込み内容：`https://arigato-3ipecjbnha-an.a.run.app/zone/{zone_id}`
- 書き込みツール：ESP32 + PN532（MicroPython）
- 書き込み保護：NTAG215のパスワード保護機能を使用

---

## 研究への橋（thesis/主張台帳 との接続）

この電子制作は趣味ではなく、論文の主張の**証拠づくり**である。
いまこの実機が背負っている主張（`../../thesis/主張台帳.md` の番号）：

| 台帳# | 主張 | この実機で何を測れば証拠になるか |
|---|---|---|
| #5 | 表情だけで「機嫌を直したい」と手が出る | キッチン設置後の世話イベント数・M/N値の推移 |
| #6 | 人ごとの地霊は1キャラより愛着を強める | 地霊あり/なし・人ごとの反応差（設置後） |

**ルール：研究の証拠になりうる結果（実測値・観察・ユーザーの反応）が出たら、
その日のログ（`../../secretary/logs/YYYY-MM-DD.md`）に `【台帳#N】` タグを付けて記録する。**
例：`【台帳#5】統合機を仮設置、初日の世話イベント3件（内訳…）`
→ thesis セッションが起動時にこのタグを拾い、タグの昇格（未検証→揺れる→固い）を判断する。
逆に、thesis 側で決まった実験設計（A-B-A'・計測項目・閾値）は、ここでの実装タスクとして降りてくる。

---

## よくある注意点

- `thanks.py` のルート順：`/welcome` は必ず `/{maintenance_id}` より前に定義する
- 写真URLはGCSの完全URL。`/maintenance/photo/` プレフィックスを付けない
- GCS バケットはbucket-level IAMで公開（blob.make_public()は不要・エラーになる）
- Firestore の `.where() + .order_by()` は複合インデックスが必要。クライアントソートで回避
