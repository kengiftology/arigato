# 本人のいま（進行役が更新・ここだけ見ればよい）

最終更新：9/23 21:5x

## いますぐできること（順番は上から）
0. **GitHub の Secrets に `NOTE_KEY` を入れる**（2分）：値は B が用意したメモ `C:/Users/kengk/.arigato/NOTE_KEY_これをGitHubに入れる.txt`。リポジトリ arigato → Settings → Secrets and variables → Actions → New repository secret（名前 `NOTE_KEY`）。次の入れ替えで観察メモの口が戻る。入れたら進行役へ「入れた」
0'. **`research-os` の git の接続先に GitHub のトークン（ghp_…）が平文で入っています**（`git remote -v` で見える）。リポジトリは非公開・ログも 4 月から push されていないので漏れてはいませんが、**そのトークンは GitHub で失効させて作り直し**、接続先は `https://github.com/kengiftology/research-os.git`（トークンなし）に戻すのが安全。手順：GitHub → Settings → Developer settings → Personal access tokens → 該当を Delete。そのあと `git -C C:/Users/kengk/research-os remote set-url origin https://github.com/kengiftology/research-os.git`
1. **A の3問に答える**（A のトークでも、進行役でもよい）：p07 はあなた？／p03 と p13 は同じ人？／p13 は「飛鳥」さん？
2. **GitHub サポートに履歴の消去を頼む**（あなたのアカウントからしか出せない）
   https://support.github.com/contact →
   「Repository: kengiftology/arigato. Please remove the dangling commits a4c9430, 1df2a8d, 23cc545, d10cfa4 and run garbage collection. They were force-pushed away because they contained personal information (interview notes with real names) committed by mistake.」
3. **紙の掲示の日付**：写真を残す期限を 9/30 に延ばしたので、「9/23 まで」とあれば直す
4. （急がない）**Google Cloud の稼働監視**：Cloud Monitoring → Uptime check → `https://arigato-3ipecjbnha-an.a.run.app/spirit/health?strict=1` を5分おき・落ちたらメール。5分

## キッチンに行くときに
- 行く前に進行役へ一言 → D が `listen 1` を送り、C3 が25秒間「考えている顔＋揺れる？」になる。口の動きは喋るときに見える。**見た感想を一言**（顔の向き・？の位置・口の大きさ）

## 今夜
- **ルーターの合言葉の変更は取り下げ**（あなたの決定 21:2x・B のトーク：ルーターの権限が無く、合言葉は研究室で周知）。代わりに C3 の書き込みの合言葉と NOTE_KEY を新しい値にした（3つとも別の値に）。記録が止まる作業は無し
- D が 21:41 に C3 を焼いた（書き込みの合言葉は新しい値・古い値は拒まれることを確認）。**今夜あなたの手が要る作業は無し**

## 明日
- **掲示 v4 の紙の差し替え**（C が文案を作る）：会話が「来訪ごとに1回話しかけ、そのあいだ耳を開ける」に変わるため。C の実装（1日）と同時
- 朝、A から「夜のまとめ直し（04:00）」の結果（まとまった組があれば写真つき）。**戻すときは進行役へ「戻して」**（`unmerge` で元に戻せる）

## いま待っているもの（あなたは何もしなくてよい）
- push の列は空。B の合言葉の分離は 16:48 に本番へ入った（**観察メモの口は `NOTE_KEY` を入れるまで 503**）。C の思い出の直しは 16:33 に本番へ。A の戻せる merge は 16:1x に本番へ入り、04:00 の自動まとめ直しも予約済み。B の向きの守りは 16:04 に本番へ入った
- D：17:50 に人感の測り直しの結果
- B：合言葉の手順書（見込み時間つき）
