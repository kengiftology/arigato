# いま触っているもの（トークどうしの札）

**10/5 までの計画は `docs/10月5日までの計画_2026-09-24.md`。タスクの番号（大.中.小）は Notion のタスクDB と同じ。**

同じ日に複数のトークを並行して開くときの、**担当の札**。
9/16 に、2つのトークが同じ `bridge/tapo_bridge.py` を同時に書き換えかけたので作った。
9/19 00:30、トークが5つになったので**持ち場**の形に作り直した（整理は「進行役」のトーク）。

---

## 1. 鍵（同時に1トークしか持てないもの）

**この3つだけが取り合いになる。持ったら名前と時刻を書き、放したら「空き」に戻す。**

| 鍵 | いま持っている人 | いつから |
|---|---|---|
| **ラズパイ本体**（ファイル書き換え・再起動・電源） | 研究トークB | 9/18 夜〜。**いま橋渡しはPC（`~/.arigato/bridge_pc`）で、ラズパイ側は停止中。PCを眠らせない。**AR9271 が届いたら戻す |
| **本番への入れ替え（git push）** | 空き（A `578df94` 22:41 入れ替え成功：確定線 0.40・新規登録 3枚5秒・p13 作り直し・04:00 に掃除→まとめ直し。9/23 終了・全部の鍵が空き） | ― |
| **C3 への書き込み（焼き直し）** | 空き（D 21:41 焼き済み・書き込みの合言葉は新しい値。古い合言葉は拒まれることを確認。以後は `spirit_secrets.h` の `OTA_PASS`） | ― |

放すときは、待っているトークに「終わった」とメッセージを送る。

---

## 2. 持ち場（トークごとの担当。**持ち場の外には手を出さない**）

| トーク | 持ち場 | 触ってよいもの | いまの状態（9/19 00:30） |
|---|---|---|---|
| **進行役**（このトーク・arigato-09） | 予定と記録。毎晩の突き合わせ。札の管理 | `docs/9月のタスク.md`、`docs/いま触っているもの.md`、`secretary/logs/` | 全体を整理中 |
| **研究トークB**（作業コピー：`.claude/worktrees/B`） | **実機と見張り**：ラズパイ・電源・停止検知・**毎日の数字** | ラズパイ本体、`bridge/`、`.github/workflows/`、`scripts/day_report.py`、**PCの橋渡し**（`C:\Users\kengk\.arigato\bridge_pc`） | **9/19 01:3x：写真が戻った。**ラズパイのWi-Fiが弱すぎる（spirit-net 50・20%落ち・映像が壊れて0枚）ので、**橋渡しをPCで動かしている**（2304×1296 で届いている。**PCをスリープさせないこと**）。ラズパイ側の tapo-bridge は停止中。AR9271 のUSB子機を購入済み、届いたらラズパイに戻す |
| **研究トークA** | **顔の精度A**：判定と覚え直しの規則・採点 | 手元の `C:\Users\kengk\facework\`。本番は**読むだけ** | 9/17夜に新規則を本番へ（`a541fbf`）。24時間で判定8回・ID 0回・間違い0回＝**写真が小さく（1280×720）なっていて測れていない**。ラズパイ待ち |
| **研究トークC** | **キャラが名前を聞く**（会話） | 手元の `server/routers/spirit_name.py`（新規）・`spirit.py` の迎え数行・`main.py` 1行・`bridge/tapo_bridge.py`。クラウドの別サービス `voicevox` | **作業コピー：`.claude/worktrees/C`（枝 worktree-C・9/19 に移った）**。手元で書き終わり・**未コミット**。次は Tapo のマイクの確認 → PC をラズパイに届く場所へ移すのを本人待ち |
| **研究トークD**（宛先名は **`研究トークD`**。9/19 昼に本人が作った。夜までは `arigato-77` だった） | **C3の態度**：C3 がなつき度の段階を受け取る＋貯金（会わなくても減らさない） | `spirit_body/`（C3のファーム）、`server/routers/spirit.py` のなつき度まわり（`_bond_now`・`/m`）。顔の照合・迎え・`spirit_name.py` は触らない | **作業コピー：`.claude/worktrees/D`（枝 worktree-D）**。手元で済み・**未push**：貯金（−1廃止・10で止める）／`/m` の末尾に段階／C3 の起動の記録。ファーム：段階3〜4の人に1回だけ喜びの絵と音・眠り中も通信・見張り（自分で再起動）。**コンパイル中。焼くのは B の「C3を触り終えた」を聞いてから**（C3 の鍵はそのとき進行役が代筆） |
| **研究トークE（マック）**（宛先名は **`研究トークE`**。9/24 に持ち場を入れ替え。旧「マックのB文書確認」＝顔の精度B・追跡構成は**取り下げ**） | **位置づけ**：事例30件→評価基準→マトリクス→位置づけの下書き（`thesis/` 側）。**常時表示**：Notion の盤面ページをマックに出しっぱなし＋1時間ごとの更新 | `thesis/`（事例・主張台帳・反論帳）、Notion 事例DB／タスクDB／盤面ページ | **カメラ・ラズパイ・C3 には触らない。**push は進行役の「どうぞ」を待つ |

---

## 3. 決まり

1. **始める前にこの札を読む。**鍵を取るときは表に書く
2. **ほかのトークの持ち場には、そのトークに確認してから**入る（宛先は ListAgents に出る名前）
3. **コミットは `git add -p` で自分の部分だけ選ぶ。**作業フォルダは全トーク共通で、他のトークの未コミットの変更が混ざっている
   （9/17、研究トークBが研究トークCの `spirit.py` の変更を誤って push しかけ、入れ替えを取り消した）
4. **トーク名は開いた最初に、役割がわかる名前にする。**名前が宛先になる。サイドバーの名前を後から変えても宛先は変わらない（9/16に確認）
5. **本番を止める作業（橋渡しの入れ替え・C3 の焼き直し・カメラの向きの変更）は、止める前に「止める・何分」を進行役へ1行送る。**利用制限などで途中で切れると、止まったまま誰も気づかない（9/22 22:57〜9/23 00:43、橋渡しが1時間45分止まった）。**進行役が居ない（返事が無い）ときは止めない**
6. **1日の終わりに、各トークが `secretary/logs/YYYY-MM-DD.md` に自分の分を追記する。**
   進行役が最後にまとめる。**9/18 はこれをせず、記録が1日ぶん空いた**

---

## 4. 本人の手が要るもの（進行役が持つ。**本人向けの短い版は `docs/本人のいま.md`**）

**決まり（9/19 本人の希望）：作業トークが本人に何か頼むときは、進行役にも1行送る**（頼んだ／済んだ／取り下げた）。
**9/21 本人の委任：本人の手が要らないことは、進行役の指示で進めてよい。**

| # | お願い | 誰から | 状態 |
|---|---|---|---|
| ~~1~~ | ~~10人の一覧・p09 の2枚~~ | A | **済み**（分類で答えが出た。p09 の2枚は入れ違い・D が正しい） |
| ~~2~~ | ~~IDが付いた223枚の分類~~ | A | **済み（9/21 14:0x）**。残り2,060枚（被覆率用）は後日 |
| ~~3~~ | ~~PC の eduroam の自動接続を切る~~ | B | **済み（9/22 22:5x）** |
| ~~3.8~~ | ~~カメラの置き方を戻す・画角が昔と違う~~ → **21:55 B が待つ向きを -0.25/+0.30 に（9/10 の見え方）**。21:43〜21:53 は試し撮りの向きのまま送っていた不具合も直した | B | 済み（直した時刻は 21:10〜21:40 の間と推定。無効の区間に影響なし） |
| ~~3.9~~ | ~~カメラの「シンクの向き」が壁を向いている~~ → 21:43 両方とも元どおり。自動の向き合わせは確かさの守りが入るまで止めたまま | B・D | 済み |
| ~~3.7~~ | ~~C3 の人感センサーの向きをシンク・作業場所へ~~ | D | **済み（9/22 22:5x）**。D が同じ測り方で取りこぼしを再測 |
| ~~3.5~~ | ~~Tapo アプリでカメラの時刻表示（OSD）をオフにする~~ → **済み（9/22 13:3x・ロゴもオフ）**（画面の角の大きな数字が顔に重なり、顔の数値を崩す。9/22 13:12 の本人の切り抜きで判明。日時はクラウドが持っているので表示は不要） | A | 待ち（9/22 13:2x〜）・1分 |
| 3.6 | **13:11 ごろ C3 の電源を抜き差ししたか**（8分41秒の停止の切り分け） | D | 返事待ち |
| ~~4~~ | ~~p01 を 9 にする試験~~ | D | **取りやめ（本人決定 9/21 17:5x）**。p01 は 4 のまま |
| ~~4.5~~ | ~~p11 が誰か~~ | A | **済み**（新しい人で正しい） |
| ~~4.7~~ | ~~D の焼き直し＋push を今日入れてよいか~~ | 進行役 | **了承（今日入れる）** |
| 4.6 | p02 の方に「9/21 朝、キャラが喜ぶ動きをしたか」を聞くか（本物の片づけで段階3に届いた初の例。聞ければ台帳#5/#6 が閉じる） | D | 本人の判断 |
| 5 | 昨夜 23:24 ごろ C3 が喜んだか（本物の場面で閉じたので、もう不要） | D | 不要 |
| ~~6~~ | ~~紙の手紙を刷り直す~~ → **本人が都度手動で直している。トークは気にしなくてよい（9/22 本人）** | — | 不要 |

---

〈9/23 15:5x 追加〉
| ~~4.8~~ | ~~ルーターの合言葉を変える~~ **取り下げ（21:2x 本人・権限なし＋周知済み）。代わりに OTA_PASS と NOTE_KEY を別の値に（B）**（同じ文字列が Wi-Fi・C3 の書き込み・観察メモの3つに使い回され、公開リポジトリに出ている。コード側の分離は B が実施中）。変えるとカメラ・C3・PC がつなぎ直しで**記録が10〜20分止まる**。いつやるか | 本人 | **今夜遅く**（B が手順を用意・進行役が呼びかけ） |
| 4.9 | **紙の掲示の日付**：写真を残す窓を 9/30 まで延ばした（本人決定 9/23）。掲示に「9/23 まで」とあれば手で直す | 本人 | 未 |
| 4.10 | **Google Cloud の稼働監視**（Uptime check＋メール・コンソール操作5分）。GitHub の予定実行は間引かれて 10 分では気づけない（中央値 3時間10分）。いまは PC から10分おきに叩き起こしているが、PC が死ぬと気づけない | 本人 | 急がない |
| 4.11 | **カメラを 30〜40cm 近づける／下げる**：-0.32 での顔幅は中央値 98px・100px 以上 42%（B 実測 484回）。向きの数値ではもう伸びない。近づければ 1.5 倍前後 | 本人 | **やらない（15:5x 決定・-0.32 のまま）** |
| ~~4.13~~ | ~~A のトークに一言「自動でまとめてよい」~~ | 本人 | **済（15:5x A のトークで「すべて許可」）** |
| 4.14 | （明日・B）声の作り置きで **1本に 422 秒**かかった回がある（他は10〜60秒）。VOICEVOX の詰まりかラズパイの負荷かを見る | B | 9/24 |
| 4.15 | **掲示 v4**：会話が「呼び名をたずねた直後だけ聞く」から「来訪ごとに1回話しかけ、そのあいだ耳を開ける（最長3分）」に変わるので、紙と QR の先の両方を直す。文案は C が作る → 本人が紙を差し替え | 本人 | C の実装と同時 |
| 4.12 | **C3 の画面の絵を目で確認**（考えている顔・揺れる「？」・口）：キッチンに行く前に一言 → D が `listen 1` で 25 秒その顔に | 本人 | 未 |

## 4.5 ネットワークの覚え書き（9/19）

- **カメラ（192.168.0.230）と C3（192.168.0.233）に届くのは TP-Link_C452 側だけ。**
- **spirit-net は大学側に橋渡しされていて、192.168.0.x はもらえない**（研究トークBの観察：PC に大学の IPv6 が付いた）。spirit-net につないでもカメラには届かない
- マックも、届いているときは 192.168.0.19（ゲートウェイ 192.168.0.1）。マックのトークには回線の名前が見えていなかった（macOS が SSID を伏せる）ので、上と矛盾しない
- **PC もマックも、黙って大学の Wi-Fi（eduroam 等）に移ることがある**（9/19：PC 16:42、マック 12:50）。移ると写真・映像が止まる。ping だけは通ることがある（大学側の別機器が同じ番号を持っている）ので、**ping が通る＝カメラに届いている、ではない**
- PC には 9/19 夜、Wi-Fi が外れたら2分で戻す仕組みが入った（Windows のタスク `ArigatoWifiKeep`・`C:/Users/kengk/.arigato/wifi_keep.ps1`・研究トークB）。ラズパイに橋渡しを戻したら無効にする

---

## 5. 作業コピー（9/19 01:3x 決定：作業トークは1トーク1コピー）

9/17 に研究トークBが研究トークCの未コミットの変更を push しかけた根は、**4つのトークが同じ作業フォルダで動いていた**こと。
これからは **作業トークは自分の作業コピー（git worktree）を持ち、共通フォルダには進行役しか居ない**。

```
arigato/                        ← 進行役だけ（枝 main）。docs・札・logs はここが正
arigato/.claude/worktrees/B/    ← 研究トークB（枝は EnterWorktree が作る）
arigato/.claude/worktrees/C/    ← 研究トークC（同上）
マック                          ← 自分のクローン。push は 9/22 の判断まで無し
```

### 入り方（各トークが自分でやる。1回だけ）
1. 共通フォルダに**自分の未コミットの変更があれば**、自分のファイルだけ退避：`git stash push -u -m "<自分の名前>" -- <自分のファイル…>`
2. `EnterWorktree`（name は自分の役割名：`B`・`C`）。origin/main から新しい枝で作業コピーができ、トークの居場所がそこに移る
3. 退避したものを戻す：`git stash pop`（stash はコピー間で共通）
4. 札の自分の行に「作業コピー：`.claude/worktrees/◯`」と書く

### 作業コピーに移ったあとの決まり
- **札と作業ログは、共通フォルダ側の絶対パスが正**（作業コピーの中の `docs/` は自分の枝の写しで、他のトークからは見えない）
  - ※9/19 12:5x 研究トークDで判明：**作業コピーに入ると、編集の道具（Edit/Write）は共通フォルダ側を書けない**。
    → **札は進行役が代筆する**（「自分の行をこう変えて」と1行送る）。**ログは Bash の追記（`cat >> …`）で書く**。それも通らなければ文面を進行役に送る
  - 札：`C:\Users\kengk\research-os\work\arigato\docs\いま触っているもの.md`
  - ログ：`C:\Users\kengk\research-os\secretary\logs\YYYY-MM-DD.md`
  - `docs/9月のタスク.md` は進行役の持ち場。**書き換えず、進行役にメッセージで「これが終わった」と送る**
- 自分の枝には自由に commit する（他人の変更は混ざらないので `git add -p` は要らなくなる）
- ラズパイへ置くときは、**自分の作業コピーのファイル**を置く（他人の未コミットの塊が混ざらない）

### 本番へ入れる（push）とき
1. 札の鍵「push」を取る
2. `git fetch origin && git rebase origin/main`（他のトークの分を下に敷く）→ 手元で動くことを確かめる
3. `git push origin HEAD:main`（`server/` `pwa/` を含めば配信が走る。`deploy.yml` が入れ替え後に主な口を自動で確かめる）
4. 鍵を放し、**進行役に「push した」と送る**（進行役が共通フォルダで `git pull --ff-only` する）

### 進行役の決まり
- docs を commit する前に `git pull --ff-only`（作業トークの push で main が進んでいる）
- 共通フォルダに残っている**持ち主のいない古い未コミット**（`esp32cam/`・`voice/manner.py`・`TODO.md`・`docs/設置手順_2026-08-29.md`。7〜8月のもの）は、当面そのまま。持ち主が分かったらそのトークに引き取らせる
