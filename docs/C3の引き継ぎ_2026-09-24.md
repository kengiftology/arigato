# C3（キッチンの地霊の体）の引き継ぎ — 研究トークD・2026-09-24

次に C3 を触る人へ。**踏むと痛い順**に書く。

---

## 1. 焼き方（無線で焼ける。線をつなぐ必要はない）

```
arduino-cli compile --fqbn esp32:esp32:XIAO_ESP32C3 \
  --build-path spirit_body/build/esp32.esp32.XIAO_ESP32C3 spirit_body

python <Arduino15>/packages/esp32/hardware/esp32/3.3.10/tools/espota.py \
  -i 192.168.0.233 -p 3232 -a <合言葉> \
  -f spirit_body/build/esp32.esp32.XIAO_ESP32C3/spirit_body.ino.bin
```

- **合言葉は `C:/Users/kengk/.arigato/spirit_secrets.h` の `OTA_PASS`。**9/23 21:41 に変えた。
  それ以前の値（`40568478`）は**もう通らない**。値は会話やコミットに出さない
- 同じファイルに Wi-Fi の設定も入っている。**git には上げない。**無いとコンパイルが
  `#error` で止まる（黙って通らないので安全）
- **キッチンが空いているときに焼く。**約1分、画面が止まる
- 転送が途中で切れることがある（9/23 に1回）。その場合 **C3 は古いままで無事**。やり直せばよい

**焼く前に 8節（実際に焼いた人の勘所）を読むこと。**

## 2. 踏むと痛いところ

**① 容量が 93%（1,222,643 / 1,310,720 バイト）。残りは約88KB。**
絵は1体ぶん6つで約42KB、会話窓は1枚20KB。**絵を足す余地はない。**
人ごとに見た目を変えるなら、絵ではなく**コードでマス目を書き替える**（`buildThinking` が手本）。

**② C3 を抜くと、研究の記録が止まる。**
人感センサーも、世話の判定（Mが下がった＝片づけられた）も C3 の中にある。
**撤去期も抜かない。**画面と声だけ止める（`docs/撤去の手順_2026-09-24.md`）。

**③ 手元の命令（UDP）で変えた状態は、電源が入り直すと消える。**
`quiet on` などは Preferences に残らない。C3 は通信が5分絶えると自分で起動し直すので、
**長く保たせたい状態はクラウド側（`/spirit/m` の返事）に置く。**
（見張りの起動は3回続けて失敗すると1時間おきになる。戻るのに最大1時間かかる）

**④ 絵に重ね描きしてはいけない。**
描画は「前のコマとの違いだけ」塗る。重ねると次のコマでそこが塗り直されず、印が顔に残る。
**絵の控え（`gWork`）のマスを書き替えてから描き直す**のが正しいやり方。口も顔もこれで作っている。

**⑤ `/spirit/m` の返事で落ちると、C3 が値を受け取れず見張りの再起動を繰り返す。**
返事を組み立てるところは、形が違っても必ず安全な値に倒すこと（9/22・9/23 に2回踏んだ）。

## 3. いま入っているもの（**9/24 21:02 に焼いた版**。容量 1,223,781 / 1,310,720・93%）

| | |
|---|---|
| `/spirit/m` の返事 | `点 N 旗 段階 聞いているか 消すか`（6つ）。**古い C3 は前の4つだけ読むので壊れない** |
| 気分（しょんぼり／ふだん） | **いまの散らかり具合だけ**で決まる（0.30以上でしょんぼり）。**時計は見ない。人が居る間は塗り替えない**（料理中に顔が曇ると、その人を責めることになる）。9/24 本人の決定 |
| 撤去期 | `HIDE 0/1/2`。記憶に残るので、起動し直しても顔は出ない（6節） |
| 砂嵐の直し | 半端な1バイトを次へ持ち越す。回数は `stat` の `ODD`（9節） |
| 聞いている顔 | 目を1マス上と右へ・口を小さく・頭の右上に「？」が300ミリ秒ごとに上下 |
| 聞いている状態 | 無線 `listen 1`／`listen 0`（橋渡しから・0.2〜1秒）。**25秒で自動的に戻る**（合図が落ちても顔が残らない）。`/m` の5つめは**入れるのにだけ**使う（切るのに使うと10秒ごとに打ち消される・9/23 に実機で踏んだ） |
| 喋るときの口 | 3段階。日本語は音の大きさ（512バイトごと・6回ぶんでならす）、あつ森語は母音（あ・お＝大きく） |
| 段階の受け渡し | 顔を確かめてから**90秒**だけ渡す。**別の顔で止める仕掛けは既定で切**（`OTHER_FACE_GUARD = False`・本人の決定）。仕組みは残してあり1行で戻せる |
| 眠り | 10分気配なしで眠る。**クラウドとのやりとりの後**に眠る絵を出す（前は眠ると通信も止まり「C3が死んだ」ように見えた） |
| `stat` で見えるもの | `M/N/STAGE`・`NET`（最後に通った時刻）・`CARE`・`QUIET`・`ODD`・`HIDE`・`MOOD`・`LISTEN`・`PIR lastMotion`・`SRC` |

## 4. 見張り（人が起きていなくても動く）

- Windows のタスク **`ArigatoWatch`**（毎日 23:50）→ `C:/Users/kengk/.arigato/spirit_watch_daily.py`
- 出力：`C:/Users/kengk/.arigato/watch/YYYY-MM-DD.md`（毎日1枚）と `ALERT.md`（おかしい日だけ）
- 見るもの：見回りの判定・判定飛ばし・喜びと相手・段階を止めた記録・世話・C3 の起動
- **元は `scripts/spirit_watch_daily.py`。直したら `.arigato/` に写し直すこと**（タスクは写しを呼ぶ）
- 9/24 の初回で、**20時間半だれも気づいていなかった見回りの停止**を拾った

## 5. C3 が返事をしなくなったときの戻し方（**12月にいちばん効く。読んでおくこと**）

**クラウドに届かなくなった C3 は、無線の命令にもほとんど答えない。**
届かない相手への接続待ちで塞がっているため。**答えるのは、見張りに起こし直された直後などの
短い隙間だけ。**9/25 の試験では、7分のうち答えたのは1回だけだった。

→ **1回送って「送った」と思ってはいけない。届くまで打ち続ける。**
9/25 は、自動で戻す仕掛けが**1回送って先へ進む作り**だったため空振りし、
**「戻したつもりで戻っていない」**が起きた。打ち続けたら13回目（約2分）で届いた。

```
python C:/Users/kengk/.arigato/rescue_c3.py
```
・宛先を本物（`arigato-3ipecjbnha-an.a.run.app 443`）へ戻す命令を、**届くまで1秒おきに15分間**送り続ける
・届いたら `stat` を読み、`SRC` が本物に戻ったことを確かめて終わる
・元は `scripts/rescue_c3.py`。**直したら `.arigato/` に写し直すこと**
・15分打っても届かなければ、**電源の入れ直しが要る**（人の手）

**`src` で宛先を変えると記憶に残る。**起動し直しても変えた先のままなので、
**戻すまで C3 はクラウドに戻らない。**試験でここを触ったら、必ず最後に戻す。

**C3 には「起動し直せ」の命令が無い。**手元から再起動させる道は、いまは
**焼き直し（reboot を伴う）か、見張り（クラウドに5分届かないと自分で起動し直す）だけ**。
足すかどうかの判断材料：**工数 約10行＋焼き直し1回（9/24 は焼きに7回かかった）／容量 +200バイト程度（残り約87KB）／
危なさ：無線の口はもともと合言葉が無く、誰でも `scene` や `src` を打てるので、危なさは増えない。**
**薦めは「足さない」。**危ない場面（クラウドに届かない）は見張りがすでに自動で拾う。
残るのは「クラウドには届くのに C3 がおかしい」という稀な場合だけで、そのときは
**電源を抜き差しすれば2秒**で済む。**9/30 の期限直前に焼き直す危なさのほうが大きい。**

## 6. 撤去期の切り替え方（12/7 と 12/20 に打つ）

```
POST https://arigato-3ipecjbnha-an.a.run.app/spirit/hide?key=<合言葉>&level=<0|1|2>&by=<名前>
```
| level | 何が消えるか |
|---|---|
| 0 | ふつう（何も消さない） |
| 1 | **画面だけ消す**（真っ黒。声・喜びの曲はそのまま鳴る） |
| 2 | **画面と声を消す**（試験用の `mur`・`say` からも鳴らない） |

・**`level` を付けずに呼べば、いまの値を見るだけ。**打つ前にこれで確かめる
・合言葉は `C:\Users\kengk\.arigato\keys_2026-09-16.txt` の TIMELAPSE_KEY
・**打ち間違いは断られる。**`0・1・2` 以外（`-2` や `3`）は 400 で拒否され、黙って見過ごされない
・**C3 が追いつくのは 6〜13秒**（9/25 実測）。`stat` の `HIDE` の行で確かめる
・切り替えた時刻は `face_state` と `period` の両方に自動で残る。**手で記録する必要はない**
・**記憶に残るので、撤去期の途中で電源が入り直しても顔は出ない。**9/25 の試験で、
  クラウドに一切届かない状態で起きてきた C3 が `HIDE 1` を保っていることを確かめた【台帳#5】
・**C3 は絶対に抜かない**（人感も世話の判定も中にある。2節②）

## 7. 手元から C3 に話しかける（UDP 5006・シリアルと同じ文法）

`stat`／`listen 1`・`listen 0`／`hide 0|1|2`（試験用・10秒でクラウドの値に戻る）／`quiet on`・`quiet off`／`scene happy|notice|sad|sleep|hatch`／
`stage 0..4`（**人が居ることにはしない**・試験用）／`mur`（声を1回）／`say <母音hex> <mask>`／
`m 0.8`・`n 0.6`（値を手で入れる）／`care set N`／`src <host> <port>`・`eye <IP>`

返事は最後に `OK <命令>` が付く。**自分の枝で `out +=` して返すと二重になる**（9/23 に直した）。

---

## 8. 実際に焼いた人の勘所（D → C・2026-09-24 に聞き取り）

資料に書いていなかったが、やった側だけが知っていたこと。

1. **合言葉はファイルから読んで渡す。**画面にも会話にも値を出さない（→ 1節）
2. **転送が切れても C3 は古いまま無事。**`stat` が返ることを確かめて、そのままやり直せばよい
3. **焼く前に「版を見分ける手がかり」を決めておく。**D は新しく足した行（`LISTEN`／`HIDE`）が
   `stat` に出るかで見た。**「焼けたはず」で済ませると、失敗に気づけない**
4. **キッチンが空いてから。**約1分、画面が止まる。最後の人の動きは `stat` の `PIR lastMotion` で分かる
5. **焼いたら必ず実機で1往復試す。**9/23 は焼いた直後に試して「10秒で勝手に元へ戻る穴」を見つけた。
   机の上では出なかった
6. **世話の回数（`CARE`）は焼く前後で変わらないはず。**変わっていたら何かおかしいので、前後で見る

## 9. 砂嵐（2026-09-24・C が直した）

**症状**：本人「キッチンで話しかけてくれたけど、砂嵐がすごくて何を言っているか分からなかった」。

**原因**：音の部品（`ESP_I2S.cpp` 1227〜1231行）は、**渡された長さが2バイトに満たないと
黙って捨てて 0 を返す**。声は2バイトで1つの数（16ビット）なので、通信から**1バイトだけ
届いた瞬間にその1バイトが消え**、以後は上の桁と下の桁が入れ替わったまま鳴りつづける＝砂嵐。
**その再生が終わるまで直らない。**`speakCloud()` は届いた長さをそのまま渡していた。

※**「奇数だからずれる」のではない。**2バイト以上なら部品の中で最後まで書き切るので、
奇数でもずれない。**捨てられるのは、ちょうど1バイトだけ届いたときだけ**。
（最初はここを取り違えて「奇数バイトが原因」と書いた。机の上で真似たら食い違い0%になり、
部品の中身を読んで分かった。**もっともらしい筋が通っても、動かして確かめること。**）

**机の上で真似た結果**：同じ声を10通りの届き方で鳴らすと、**10回中7回が壊れた**
（本来と食い違った点 1.0〜94.6%）。余りを持ち越す形にすると**10回とも0%**。
「ときどき聞き取れて、ときどき全く分からない」という見え方とも合う。

**確かめ方（使えるやり方として残す）**：本人に「どこが悪いか」を言わせるのではなく、
**候補を3本作って鳴らし、選ばせた**——① そのまま ② 1バイトずれ ③ 受け取りが追いつかない。
本人は即答した。切り分けに数分しかかからない。
（先に疑われていた「音量 1.3 で割れている」は、声6本の波形を測って**上限に達した点0**で外れた）

**直し**：余った1バイトを次の読み取りの頭へ持ち越し、**必ず偶数バイトで流す**。
口の動きのために音の大きさを数える側も、同じ範囲（`even`）までに揃えてある。

**`oddCarries`**：持ち越しが起きた回数を数えている。`stat` に出すと、
**砂嵐がほんとうに起きていたかの証拠**になる。
**ただし 0 は「直った証拠」にならない**（その再生でたまたま奇数が来なかっただけのことがある）。
**0より大きい値が出たときだけが証拠。**
